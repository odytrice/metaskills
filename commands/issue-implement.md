---
description: Implement a planned, claimed issue from its plan ledger to a PR (or run a revision round on its PR)
---
Use the `issue-implement` skill for the issue or PR referenced by the arguments: execute the existing plan ledger in an isolated worktree and open the PR, or, when given a PR and enumerated review findings, run a revision round. Stop and report if no plan ledger exists.

$ARGUMENTS
