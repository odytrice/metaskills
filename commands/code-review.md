---
description: Review a PR/branch, or uncommitted changes when no target is given
---
Use the `code-review` skill. If the arguments name a PR, branch, or commit, run PR mode; if empty, run local mode against uncommitted working-tree changes.

$ARGUMENTS
