---
description: QA the running app with the Playwright MCP and file reproducible bugs
agent: quality-assurance
---
Act as the QA role: load the `agent-login` skill, drive the running app with the Playwright MCP (`browser_*` tools — a live browser session, not ad-hoc test scripts) against the staging environment from AGENTS.md (never production unless explicitly instructed), reproduce the behavior described by the arguments, and file only deterministically reproducible bugs using the repo issue template.

$ARGUMENTS
