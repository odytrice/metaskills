---
description: QA the running app with the Playwright MCP and file reproducible bugs
agent: quality-assurance
---
Act as the QA role: resolve auth per AGENTS.md § Agent Login (staging by default; never production unless explicitly instructed), drive the running app with the Playwright MCP (`browser_*` tools; a live browser session, not ad-hoc test scripts), reproduce the behavior described by the arguments, and file only deterministically reproducible bugs using the repo issue template.

$ARGUMENTS
