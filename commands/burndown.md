---
description: Burn down a batch of project issues with worker and review agents
---
Use the `burndown` skill to triage, plan, implement, review, and merge the batch of issues scoped by the arguments (labels, milestone, explicit issue numbers, or a board column; default is the board's Ready column).

$ARGUMENTS
