---
description: Review, commit, push, and monitor a deployment through CI/CD
---
Use the `deploy` skill to gate, ship, and monitor the deployment described by the arguments (environment and scope; production only when explicitly requested).

$ARGUMENTS
